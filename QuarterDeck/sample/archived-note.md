# Retired Note

This item sits in the **Archive** section, so its sidebar entry is greyed out. Archive is
just a section — a state for items that are done or superseded but worth keeping around.
Nothing special happens to them; the Console still renders them on demand.

Move an item here by setting its `section` to `archive` in `console.json`. Move it back out
the same way.
